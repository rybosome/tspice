{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["TSPICE_CSPICE_DIR"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "native/build/Release/*.node"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": ["dist/**/*.d.ts", "dist/**/*.d.ts.map", "dist/.tsbuildinfo"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
