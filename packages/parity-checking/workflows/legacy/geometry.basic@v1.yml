id: workflows/legacy/geometry.basic@v1
kind: workflow
setup:
  kernels:
    - $FIXTURES/basic-time
    - $FIXTURES/ephemeris-de440s
compareDefaults:
  tolAbs: 1e-12
  tolRel: 1e-12
notes:
  - Added for geometry classic parity coverage (sub-issue #482).
