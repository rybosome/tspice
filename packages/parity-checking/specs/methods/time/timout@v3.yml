schemaVersion: 3
manifest:
  id: methods/time/timout@v3
  kind: method
contract:
  contractMethod: time.timout
  canonicalMethod: time.timout
setup:
  kernels:
    - $FIXTURES/basic-time
defaults:
  compare:
    tolAbs: 1e-12
    tolRel: 1e-12
workflow:
  steps:
    - op: call
      call: self
      in:
        - $args.0
        - $args.1
cases:
  - id: time.timout
    args:
      - 0
      - YYYY-MON-DD HR:MN:SC.### ::TDB
    expect:
      ok: true
