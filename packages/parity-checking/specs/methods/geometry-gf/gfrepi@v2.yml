schemaVersion: 2
manifest:
  id: methods/geometry-gf/gfrepi@v2
  kind: method
contract:
  contractMethod: geometry-gf.gfrepi
  canonicalMethod: geometry-gf.gfrepi
  aliases: []
  result:
    type: object
    properties: {}
setup:
  kernels:
    - $FIXTURES/basic-time
    - $FIXTURES/ephemeris-de440s
defaults:
  compare:
    tolAbs: 1e-12
    tolRel: 1e-12
workflow:
  steps:
    - op: invokeLegacyCall
cases:
  - id: geometry-gf.gfrepi.empty-window
    args:
      - - window
        - 8
      - parity begin
      - parity end
    compare:
      errorShort: true
    expect:
      ok: false
      errorShort: SPICE(NOINTERVAL)
