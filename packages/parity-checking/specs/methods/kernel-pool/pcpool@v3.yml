schemaVersion: 3
manifest:
  id: methods/kernel-pool/pcpool@v3
  kind: method
contract:
  contractMethod: kernel-pool.pcpool
  canonicalMethod: kernel-pool.pcpool
setup:
  kernels:
    - $FIXTURES/kernel-pool-parity.tpc
workflow:
  steps:
    - op: call
      call: self
      in:
        - $args.0
        - $args.1
    - op: projectResult
      out: {}
cases:
  - id: pcpool-write
    args:
      0: TSPICE_PARITY_PCPOOL_WRITTEN
      1:
        - x
        - y
    expect:
      ok: true
