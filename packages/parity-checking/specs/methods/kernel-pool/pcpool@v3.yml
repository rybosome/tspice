schemaVersion: 3
manifest:
  id: methods/kernel-pool/pcpool@v3
  kind: method
contract:
  contractMethod: kernel-pool.pcpool
  canonicalMethod: kernel-pool.pcpool
setup:
  kernels:
    - $FIXTURES/kernel-pool-parity.tpc
workflow:
  steps:
    - op: call
      call: self
      in:
        - TSPICE_PARITY_PCPOOL_WRITTEN
        -
          - x
          - y
    - op: projectResult
      out: {}
cases:
  - id: pcpool-write
    expect:
      ok: true
