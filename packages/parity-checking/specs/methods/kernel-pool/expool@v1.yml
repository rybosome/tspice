id: methods/kernel-pool/expool@v1
kind: method
contractMethod: kernel-pool.expool
canonicalMethod: kernel-pool.expool
uses:
  - workflows/legacy/kernel-pool.basic@v1
cases:
  - id: expool-char-miss
    args:
      - TSPICE_PARITY_CHAR_VAR
    expect:
      ok: true
  - id: expool-found
    args:
      - TSPICE_PARITY_DOUBLE_VAR
    expect:
      ok: true
  - id: expool-missing
    args:
      - TSPICE_PARITY_MISSING_VAR
    expect:
      ok: true
