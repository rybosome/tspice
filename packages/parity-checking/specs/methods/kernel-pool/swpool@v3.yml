schemaVersion: 3
manifest:
  id: methods/kernel-pool/swpool@v3
  kind: method
contract:
  contractMethod: kernel-pool.swpool
  canonicalMethod: kernel-pool.swpool
setup:
  kernels:
    - $FIXTURES/kernel-pool-parity.tpc
workflow:
  steps:
    - op: call
      call: self
      in:
        - $args.0
        - $args.1
    - op: projectResult
      out: {}
cases:
  - id: swpool-watch
    args:
      0: TSPICE_PARITY_AGENT
      1:
        - TSPICE_PARITY_DOUBLE_VAR
    expect:
      ok: true
