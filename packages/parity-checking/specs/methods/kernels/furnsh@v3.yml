schemaVersion: 3
manifest:
  id: methods/kernels/furnsh@v3
  kind: method
contract:
  contractMethod: kernels.furnsh
  canonicalMethod: kernels.furnsh
workflow:
  steps:
    - op: call
      fn: kernels.furnsh
      in: $args
cases:
  - id: kernels.furnsh.text-kernel.bytes
    args:
      - path: $FIXTURES/kernel-pool-parity.tpc
        bytes: $FIXTURES/kernel-pool-parity.tpc
    expect:
      ok: true
