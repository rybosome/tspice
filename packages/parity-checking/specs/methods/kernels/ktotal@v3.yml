schemaVersion: 3
manifest:
  id: methods/kernels/ktotal@v3
  kind: method
contract:
  contractMethod: kernels.ktotal
  canonicalMethod: kernels.ktotal
workflow:
  steps:
    - op: call
      fn: kernels.ktotal
      in:
        - $args.0
cases:
  - id: kernels.ktotal
    args:
      - ALL
    setup:
      kernels:
        - $FIXTURES/kernel-pool-parity.tpc
    expect:
      ok: true
