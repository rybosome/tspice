schemaVersion: 3
manifest:
  id: methods/frames/ccifrm@v3
  kind: method
contract:
  contractMethod: frames.ccifrm
  canonicalMethod: frames.ccifrm
setup:
  kernels:
    - $FIXTURES/frames-minimal
defaults:
  compare:
    tolAbs: 1e-12
    tolRel: 1e-12
workflow:
  steps:
    - op: call
      call: self
      in:
        - $args.0
        - $args.1
cases:
  - id: ccifrm-j2000
    args:
      - 1
      - 1
    expect:
      ok: true
