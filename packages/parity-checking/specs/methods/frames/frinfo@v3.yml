schemaVersion: 3
manifest:
  id: methods/frames/frinfo@v3
  kind: method
contract:
  contractMethod: frames.frinfo
  canonicalMethod: frames.frinfo
setup:
  kernels:
    - $FIXTURES/frames-minimal
defaults:
  compare:
    tolAbs: 1e-12
    tolRel: 1e-12
workflow:
  steps:
    - op: call
      fn: frames.frinfo
      in:
        - $args.0
cases:
  - id: frinfo-j2000
    args:
      - 1
    expect:
      ok: true
