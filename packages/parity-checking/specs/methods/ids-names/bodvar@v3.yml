schemaVersion: 3
manifest:
  id: methods/ids-names/bodvar@v3
  kind: method
contract:
  contractMethod: ids-names.bodvar
  canonicalMethod: ids-names.bodvar
setup:
  kernels:
    - $FIXTURES/frames-minimal
    - $FIXTURES/body399-char-foo.tpc
workflow:
  steps:
    - op: callContract
cases:
  - id: bodvar-char-typed-item
    args:
      - 399
      - FOO
    expect:
      ok: true
  - id: bodvar-radii
    args:
      - 399
      - RADII
    expect:
      ok: true
