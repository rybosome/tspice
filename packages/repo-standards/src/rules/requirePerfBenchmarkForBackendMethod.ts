import type { RuleRunInput, RuleRunResult } from "./types.js";

export const id = "require-perf-benchmark-for-backend-method";

/** Placeholder rule: ensure backend-contract API changes include performance coverage. */
export function run(_input: RuleRunInput): RuleRunResult {
  return [];
}
