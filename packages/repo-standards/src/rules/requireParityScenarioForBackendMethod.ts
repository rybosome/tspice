import type { RuleRunInput, RuleRunResult } from "./types.js";

export const id = "require-parity-scenario-for-backend-method";

/** Placeholder rule: ensure backend-contract API changes include parity scenarios. */
export function run(_input: RuleRunInput): RuleRunResult {
  return [];
}
