import type { BenchmarkContractV1, NormalizeFixtureRefsOptions } from "./types.js";

/**
 * Normalize fixture references in a v1 benchmark contract (e.g. canonicalize ids/paths).
 *
 * NOTE: normalization is intentionally unimplemented in this scaffold.
 */
export function normalizeFixtureRefs(
  _contract: BenchmarkContractV1,
  _options: NormalizeFixtureRefsOptions = {},
): BenchmarkContractV1 {
  throw new Error("not implemented");
}
