{
  "name": "tspice",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "check": "pnpm run fetch:cspice && pnpm run build && pnpm run typecheck && pnpm run test",
    "fetch:cspice": "node scripts/fetch-cspice.mjs",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  }
}
